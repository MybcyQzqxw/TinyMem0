# =============================================================================
# TinyMem0 环境变量配置模板
# =============================================================================
# 说明：将此文件复制为 .env 并根据实际情况修改配置

# -----------------------------------------------------------------------------
# LLM模式选择
# -----------------------------------------------------------------------------
# 使用本地LLM还是阿里云API
# 值: "true" 或 "false"
USE_LOCAL_LLM=true

# -----------------------------------------------------------------------------
# 本地LLM配置（当USE_LOCAL_LLM=true时生效）
# -----------------------------------------------------------------------------
# 本地GGUF模型文件路径（必需）
# 示例: LOCAL_MODEL_PATH=E:/models/qwen2-7b-instruct-q4_0.gguf
LOCAL_MODEL_PATH=models/Mistral-7B-Instruct-v0.3.Q4_K_M.gguf

# 本地嵌入模型名称（可选，默认: BAAI/bge-small-zh-v1.5）
# 其他选项: sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
LOCAL_EMBEDDING_MODEL=BAAI/bge-small-zh-v1.5

# 嵌入向量维度（可选，默认: 512用于本地模型，1536用于阿里云）
EMBEDDING_DIM=512

# -----------------------------------------------------------------------------
# 阿里云API配置（当USE_LOCAL_LLM=false时生效）
# -----------------------------------------------------------------------------
# 阿里云DashScope API密钥
# 获取地址: https://dashscope.console.aliyun.com/apiKey
# DASHSCOPE_API_KEY=your_api_key_here

# -----------------------------------------------------------------------------
# LoCoMo评测配置
# -----------------------------------------------------------------------------
# 评测样本数量限制（可选，默认: None表示全部评测）
# MAX_SAMPLES=100

# 是否打印详细评测日志（可选，默认: false）
# VERBOSE_EVAL=false
